# 星枢 StarPivot AstroAI — 部署依赖清单
# 安装: uv pip install -r requirements.txt (或 pip install -r requirements.txt)

fastapi>=0.110
uvicorn[standard]>=0.29        # 必须带[standard] — WebSocket支持
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0
websockets>=12.0

# Python 3.10 - 3.12 x64
# 外部依赖: N.I.N.A + Advanced-API插件(1888端口)
# 模型: .env 配 DEEPSEEK_API_KEY / ZHIPU_API_KEY / STARPIVOT_CHANNELS(任意OpenAI兼容)
